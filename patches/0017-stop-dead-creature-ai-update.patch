diff --git a/src/server/game/Entities/Creature/Creature.cpp b/src/server/game/Entities/Creature/Creature.cpp
--- a/src/server/game/Entities/Creature/Creature.cpp
+++ b/src/server/game/Entities/Creature/Creature.cpp
@@ -1027,6 +1027,11 @@ void Creature::Update(uint32 diff)
         case ALIVE:
         {
             Unit::Update(diff);
+
+            // Unit::Update can kill this creature through periodic damage. Never let dead
+            // creatures run AI, which can restart chase movement and combat after death cleanup.
+            if (!isAlive())
+                break;
 
             // if creature is charmed, switch to charmed AI (and back)
             if (NeedChangeAI)
@@ -1067,7 +1073,7 @@ void Creature::Update(uint32 diff)
                     m_regenTimerCount -= m_sendInterval;
             }
 
-            // creature can be dead after Unit::Update call
+            // creature can be dead after its AI update
             // CORPSE/DEAD state will processed at next tick (in other case death timer will be updated unexpectedly)
             if (!isAlive())
                 break;
